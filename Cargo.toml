[package]
name = "autosqueeze"
version = "0.1.0"
edition = "2021"
description = "Autonomous compression research — an AI agent searches for novel compression algorithms"
license = "MIT"

[[bin]]
name = "benchmark"
path = "src/benchmark.rs"

[[bin]]
name = "compress"
path = "src/compress.rs"
